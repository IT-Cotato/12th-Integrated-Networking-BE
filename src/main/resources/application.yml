spring:
  application:
    name: networking2
  jpa:
    hibernate:
      ddl-auto: none
    properties:
      hibernate:
        format_sql: true
    database: mysql
    database-platform: org.hibernate.dialect.MySQL8Dialect
    show-sql: true
    generate-ddl: true
  config:
    import: optional:file:.env.properties
  profiles:
    active: dev
    include: oauth

